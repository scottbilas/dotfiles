[include]
    path = ~/.config/git/config

# the rest of this file is overrides for windows
# TODO: it's also overrides for cygwin, which shares home with windows. figure out how to split..because diff-so-fancy etc. is ok in cyggy (or implement diff-so-fancy in windows)

[core]
    hideDotFiles = false
    pager = less
    editor = subl --new-window --wait

[diff]
    tool = bc
[difftool]
    prompt = false
[difftool "bc"]
    path = c:/users/scott/scoop/apps/beyondcompare/current/BComp.exe

[merge]
    tool = bc
[mergetool "bc"]
    path = c:/users/scott/scoop/apps/beyondcompare/current/BComp.exe
[credential]
	helper = manager
